import { Observable } from "rxjs";
import { Request } from "express";

const updateUser = async (req:Request) => {
    return new Observable((suscriber)=>{
        // Function for database actions ...
    })
};

export default updateUser
