import bcrypt from "bcrypt";

// export const checkHash = async (hash:string,providePassword:string) => {
//        await bcrypt.compare(providePassword,hash,(err,result)=>{
//            if(err){
//                colors.red('Error comparing password '+ err.message)
//            }else{
//                return result
//         }
//     })
// }




